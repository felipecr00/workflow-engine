{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "workflow-engine.serviceAccountName" . }}
  labels:
    {{- include "workflow-engine.labels" . | nindent 4 }}
{{- end -}}
